{% extends 'layout.html.twig' %}

{% block content %}
    <div class="article">
        <h1>{{article.title}}</h1>
        <img src="{{article.img}}" alt="{{article.title}}">
        <p>
            {{article.description}}
        </p>
    </div>
{% endblock %}

